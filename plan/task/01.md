# Task — document agent-studio (bootstrap)

Source: `.` · stack: `auto` · kind: `repo`

Follow the FIXED prompt `assets/prompts/bootstrap.md` from the ralph-vault skill, writing into `vault/repos/agent-studio/` and its repo-card.

## Subtasks

- [ ] Detect stack (if `auto`) and seed the per-section task list from the matching `assets/stack-tasks/<stack>.md`.
- [ ] Generate the per-section docs under `repos/agent-studio/`, stamping `source_globs` (the source paths each section covers, relative to the source).
- [ ] Write the tier-1 repo-card `agent-context/repo-cards/agent-studio.md`.
- [ ] Run `gv.py validate --vault <vault>`; fix any reported error.
- [ ] [juez] verify the docs against the source (faithfulness, no copied code, frontmatter present).
- [ ] Close: `gv.py mark-synced --vault <vault> --repo agent-studio` (sets phase=done, records the baseline commit + logs it).

# Task — dependencies (cross-repo graph)

Catalogue external infra + providers (reverse index of consuming repos). Run **after** the repo tasks in this plan are done.

Follow the FIXED prompt `assets/prompts/dependencies.md` from the ralph-vault skill.

Registered repos: agent-studio.

## Subtasks

- [ ] Write one file per external piece under `infrastructure/` (deployed) or `technologies/` (third-party SDK/API), each listing the consuming repos with evidence.
- [ ] Run `gv.py validate --vault <vault>`; fix any error.
- [ ] [juez] verify the reverse index is complete and no secrets are pasted.
